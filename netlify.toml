[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Disables the smart detection for secrets that flags public Firebase/Google keys
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
